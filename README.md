## Premium fitness tracker app (workouts, goals, progress charts, activity tracking simulation, food photo calorie estimator simulation)

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
